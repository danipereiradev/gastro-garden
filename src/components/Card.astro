---
import Button from './Button.astro';
const { title, description, imageUrl, buttonText } = Astro.props;
---

<article class="flex md:w-1/4 flex-col justify-between rounded-xl text-center shadow-xl">
  <img class="w-full" src={imageUrl} alt="" />
  <div class="flex flex-col p-8 text-sm">
    <h4 class='text-2xl'>{title}</h4>
    <p>{description}</p>
    <Button buttonText="Ver más" customClass="place-self-center" />
  </div>
</article>
